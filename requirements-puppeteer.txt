# The pyppeteer mirror. Effectively unmaintained upstream; here for parity.
#   pip install -r requirements.txt -r requirements-puppeteer.txt
#
# Install it in its OWN virtualenv: playwright and pyppeteer declare
# mutually unsatisfiable pins (pyee <12 vs >=13) and pyppeteer and selenium
# collide on urllib3 (<2.0 vs >=2.6). They do run side by side in practice
# because neither library touches the incompatible part, but `pip check`
# reports the conflict and pip may resolve it by downgrading something you
# wanted.
pyppeteer>=1.0.2
